import React, { Component } from "react";

export default class AcquiredResource extends Component {
  constructor(props) {
    super(props);

    // this.configSetup();

    this.state = {};
  }
  // resource, resource type, date acquired
  render() {
    return (
      <div>
       
      </div>
    );
  }
}
